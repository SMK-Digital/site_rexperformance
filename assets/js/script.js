document.addEventListener('DOMContentLoaded', () => {
    // Esconder o spinner quando o DOM estiver carregado
    const spinner = document.querySelector('.spinner-border');
    spinner.style.display = 'none'; // Esconder o spinner
});



